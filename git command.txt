git config --global user.name "YourUserName"
git config --global user.email "YourEmail"

git init
git add .
git commit -m "my first commit"
git remote add origin https://github.com/hashiim/shopy.git
git push -u origin main

git status
git branch -M main
git clone https://github.com/hashiim/shopy.git
git reset head filename		remove frome stage  


-BottomNavigationBar 
-provider
-exitShowDialog
-model
-local crud
-Dismissible
-GestureDetector
-Snackbar
